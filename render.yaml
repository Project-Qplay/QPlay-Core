services:
  - type: web
    name: qplay-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/production_server.py
    branch: main
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
